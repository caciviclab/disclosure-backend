upstream disclosurebackend {
  server 127.0.0.1:8080 fail_timeout=0;
}

server {
  listen 80;
  server_name opencal.opendisclosure.io;

  location / {
    include uwsgi_params;
    uwsgi_pass http://disclosurebackend;
  }
}
